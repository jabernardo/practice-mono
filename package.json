{
  "name": "practice-mono",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "start": "lerna run transpile && lerna run start --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}